# --- Core Dependencies (FIXED) ---
# Updated versions for code-specific models

# Sentence Transformers (with nomic support)
sentence-transformers>=3.0.0

# Nomic library (for CodeRankEmbed and nomic-embed-code)
nomic>=3.0.0

# PyTorch (GPU support recommended)
torch>=2.0.0

# NumPy
numpy>=1.24.0

# HuggingFace Hub (for model downloads)
huggingface-hub>=0.19.0

# Transformers (required by sentence-transformers)
transformers>=4.35.0

# einops (required by some models)
einops>=0.7.0

# --- Development Dependencies ---
# Tools for testing and code formatting
pytest>=7.0.0
black>=23.0.0

# --- Optional: Colab Integration ---
# These are auto-installed in Colab but listed for completeness
# google-colab  # Only in Colab
# ipython       # Only in Colab